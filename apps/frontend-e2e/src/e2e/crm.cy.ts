describe('The Data CRM Feature', () => {
  beforeEach(() => {
    cy.visit('/data/crm');
  });

  describe('The Happy Path', () => {
    it('loads', () => {
      // left intentionally blank.
    });
  });
});
